// Placeholder entry file to satisfy legacy requests to /src/index.jsx
// No-op to avoid 404s when an old service worker or cached HTML references this path.
export default function noop() {
  // noop
}
